{
  "version": 2,
  "buildCommand": "vite build && esbuild server-fn.ts --platform=node --packages=external --bundle --format=esm --outfile=api/index.js",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
